# Next.js + TypeScript

This template provides a minimal Next.js Pages Router setup with TypeScript.

Routes live in the `pages` directory. Global styles are imported from
`pages/_app.tsx`.

## Getting Started

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.
